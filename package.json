{
  "name": "rt-api",
  "version": "1.0.0",
  "type": "module",
  "description": "API for running Retention Troubleshooter via POST requests",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install --with-deps"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.55.0"
  },
  "engines": {
    "node": "18.x"
  }
}
